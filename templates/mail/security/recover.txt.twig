{% trans_default_domain 'security_mail' %}

{{ 'recover title' | trans  }}

{{ 'recover message' | trans  }}

{{ constant('App\\SiteConfig::SITEURL') }}{{ path('security_recover_validation') }}?email={{ data.email }}&token={{ data.token }}
