{% trans_default_domain 'security_mail' %}

{{ 'register title' | trans }}

{{ 'register message' | trans }}

{{ constant('App\\SiteConfig::SITEURL') }}{{ path('security_register_validation') }}?email={{ data.email }}&token={{ data.token }}
