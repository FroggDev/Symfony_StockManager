# ENV TEST !


# will use a fake database loaded in memory
#doctrine:
#    dbal:
#        driver:   pdo_sqlite
#        memory:   true

doctrine:
    dbal:
        # With Symfony 3.3, remove the `resolve:` prefix
        url: 'mysql://root:root@127.0.0.1:3306/stockmanagerTEST'
